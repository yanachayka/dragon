// The app's "today". This is the ONLY source of the current date in the app.
// To run the app on the real date, replace the literal below with an ISO string
// produced from the system clock — nothing else needs to change.
export const TODAY = '2026-03-29'
